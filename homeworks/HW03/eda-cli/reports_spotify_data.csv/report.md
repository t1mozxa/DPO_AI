# EDA-отчёт по датасету spotify.csv

Исходный файл: `spotify_data.csv`

Строк: **8582**, столбцов: **15**

## Качество данных (эвристики)

- Оценка качества: **0.51**
- Макс. доля пропусков по колонке: **39.16%**
- Слишком мало строк: **False**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Наличие константных колонок: **False**
- Наличие категориальных признаков с высокой кардинальностью: **True**
- Наличие числовых колонок с большим количеством нулей: **False**
- Наличие подозрительных дубликатов ID: **False**

## Параметры отчёта

- Минимальная доля пропусков для проблемных колонок: **15.00%**
- Количество top-категорий: **10**
- Максимум колонок для гистограмм: **10**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
