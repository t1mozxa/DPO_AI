# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.64**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Наличие константных колонок: **False**
- Наличие категориальных признаков с высокой кардинальностью: **False**
- Наличие числовых колонок с большим количеством нулей: **False**
- Наличие подозрительных дубликатов ID: **True**

## Параметры отчёта

- Минимальная доля пропусков для проблемных колонок: **10.00%**
- Количество top-категорий: **5**
- Максимум колонок для гистограмм: **6**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
